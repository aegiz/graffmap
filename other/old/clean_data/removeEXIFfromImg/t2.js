// exiftool -all= ./input
const portraitJSON = require("./output.json");

// make new array containing an the id of the graff and booleans with image1 2 3
for (var i = portraitJSON.length - 1; i >= 0; i--) {
	var item = portraitJSON[i];
	
}